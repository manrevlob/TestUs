<div class="forms center-block" style="width: 60%;">
  <%= form_for @suite, :html => {:class => 'form-horizontal'} do |f| %>
      <% @suite.errors.full_messages.each do |m| %>
          <div class="error">
            * <%= m %>
          </div>
      <% end %>
      <div class="form-group">
        <%= f.label :fname, t('suiteName'), class: 'col-sm-2 control-label' %>
        <div class="col-sm-10">
          <%= f.text_field :name, placeholder: t('suiteName'), class: 'form-control' %>
        </div>
      </div>

      <div class="form-group">
        <%= f.label :fdescription, t('suiteDescription'), class: 'col-sm-2 control-label' %>
        <div class="col-sm-10">
          <%= f.text_area :description, placeholder: t('suiteDescription'), class: 'form-control' %>
        </div>
      </div>

      <div class="col-sm-10 col-sm-offset-2">
        <%= f.submit t('save'), class: "btn btn-default btn-primary" %>
        <%= link_to t('back'), suites_path, class: "btn btn-default" %>
      </div>

  <% end %>
</div>